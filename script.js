const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const menu=$('.menu'),nav=$('.nav-links');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});$$('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const theme=$('.theme');const saved=localStorage.getItem('amr-theme');if(saved==='light')document.body.classList.add('light');theme?.addEventListener('click',()=>{document.body.classList.toggle('light');localStorage.setItem('amr-theme',document.body.classList.contains('light')?'light':'dark')});const obs=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');obs.unobserve(e.target)}}),{threshold:.12});$$('.reveal').forEach(e=>obs.observe(e));const glow=$('.cursor-glow');window.addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'},{passive:true});$$('.filter').forEach(button=>button.addEventListener('click',()=>{const value=button.dataset.filter;$$('.filter').forEach(b=>b.classList.remove('active'));button.classList.add('active');$$('.project').forEach(card=>{const show=value==='all'||card.dataset.tags.includes(value);card.classList.toggle('hidden',!show)})}));window.addEventListener('scroll',()=>{document.documentElement.style.setProperty('--scroll',window.scrollY)}, {passive:true});